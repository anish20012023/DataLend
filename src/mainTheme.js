import { createTheme } from "@mui/material";

export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#543F99",
    },
    text: {
      primary: "#543F99",
    },
    drawerBorder: " #FD35F9",
    background: {
      default: "#E5E2F0",
    },
  },
});
